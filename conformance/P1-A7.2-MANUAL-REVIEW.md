# P1-A7.2 portable error adapter boundary review

Revision reviewed: `EIGIIB-P1-A7.2-1.0`.

- The Python reference, independent Go and external Go adapters are separate executable routes.
- The first slice contains one positive seed and five negative JSON, UTF-8, base64 and path vectors.
- All route outputs use the same closed portable result carrier.
- Duplicate JSON members and trailing data are rejected as `syntax.invalid-json`.
- Invalid UTF-8 is rejected before JSON parsing as `syntax.invalid-utf8`.
- Non-canonical base64 and unsafe paths are rejected at their declared boundaries.
- The external route rejects this slice before COSE parsing; no `go-cose` invocation is claimed.
- The canonical report contains eighteen observations and excludes parser-specific messages.
- Identity, projection, DSSE, CBOR, COSE, Receipt and multi-defect precedence remain open.
- P1-A7.2 does not replace P1-A1 through P1-A6 authorities.

No deviation is accepted by this attestation.
