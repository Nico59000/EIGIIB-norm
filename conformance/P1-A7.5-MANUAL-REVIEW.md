# P1-A7.5 deterministic CBOR and COSE review boundary

Revision reviewed: `EIGIIB-P1-A7.5-1.0`.

- A7.1 through A7.4 artifacts remain unchanged and are replayed before A7.5.
- The positive source is the exact P1-A3 Signed Statement and supplied issuer public key.
- The source COSE_Sign1 and issuer SPKI identities are re-observed before mutation.
- The portable order is top-level CBOR, COSE structure, protected CBOR, payload CBOR, headers, then signature.
- Non-minimal tag encoding and non-canonical protected or payload map order map to `cbor.nondeterministic`.
- Wrong tag or non-byte-string protected header maps to `cose.invalid-structure`.
- Unsupported algorithm and unknown critical labels map to `cose.unsupported-header`.
- A malformed critical carrier maps to `cose.invalid-structure`.
- The multi-defect vector must return `cbor.nondeterministic` before `cose.unsupported-header`.
- The external route invokes `go-cose` only after portable CBOR and header gates pass.
- Thirty canonical observations are required across three routes.
- Receipt detached binding, proof, coordinates and root remain outside A7.5.
- No production interoperability, trust, authorization or source-authenticity claim is made.

No deviation is accepted by this attestation.
