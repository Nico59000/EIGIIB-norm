# M0-A12 manual review authority

## Exact lineage

- base branch: `agent/m0-a11-external-control-domain-registry-immutable-preservation-channel-bootstrap-independent-observation-campaign-lapse-detection`;
- exact base head: `148e3e9d06ce791b90e2816d77f5045ebeac0793`;
- M0-A10 anchor: `2891265f04a5d9a4d69c134fa48881b0ed93fe13`;
- no historical E14, E15, E16, P1 or M0 head may be moved or rewritten.

## Baseline review

The initial PR may be accepted as a valid activation harness only when:

1. the typed decision label is `NF`;
2. all provider resources and credentials remain `unbound`;
3. the evidence ledger is empty;
4. no `evidence/m0-a12` directory is committed;
5. the synthetic signature vector exists only in temporary test storage;
6. `--require-activated` exits with code `2`;
7. all schemas, adapters, checker and freeze validate on three platforms.

## Live closure review

Point-in-time closure additionally requires:

1. three real signed control-domain attestations;
2. 54 distinct diversity cells with resolvable evidence;
3. two immutable version-specific channel records;
4. retention readback and two retention-attributed delete denials per channel;
5. two independent exact object readbacks;
6. a campaign anchor after all bindings;
7. sequence-1 observation after activation;
8. a valid observer detached signature;
9. a final `T` report;
10. all workflows on one exact final head in success.

## Prohibited interpretation

A selected provider profile, a passing synthetic test, an adapter plan, a
created account, versioning alone, an ACL denial, a schedule definition or a
single unsigned readback is not point-in-time activation evidence.

Even a successful M0-A12 closure is not long-horizon preservation evidence and
does not authorize E17 adoption.
