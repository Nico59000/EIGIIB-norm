# P1-A7.4 manifest, DSSE and signature-carrier boundary review

Revision reviewed: `EIGIIB-P1-A7.4-1.0`.

- P1-A7.1 through P1-A7.3 artifacts remain unchanged and are replayed as prior closures.
- The A7.4 corpus binds the exact P1-A2 bundle and supplied Ed25519 public key by SHA-256 and byte length.
- Manifest membership, order and exact member identities are checked before DSSE or signature processing.
- The DSSE payload type is fixed to `application/vnd.in-toto+json` and the signed bytes use exact DSSE PAE.
- A malformed signature carrier is distinct from a parsed signature that fails cryptographic verification.
- The multi-defect vector combines manifest-order and invalid-signature defects and must return `manifest.invalid`.
- The independent Go route uses the Go standard library; the reference route uses OpenSSL.
- The external route is observed at its pre-COSE DSSE boundary; no `go-cose` invocation is claimed.
- Thirty canonical observations are required: one positive and nine negative inputs across three routes.
- Signature validity does not establish signer trust, authorization, claim truth, transparency inclusion or trusted time.
- P1-A7.4 does not close deterministic CBOR, COSE-header or Receipt negative families.

No deviation is accepted by this attestation.
