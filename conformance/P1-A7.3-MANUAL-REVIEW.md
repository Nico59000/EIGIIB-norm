# P1-A7.3 structural replay boundary review

Revision reviewed: `EIGIIB-P1-A7.3-1.0`.

- P1-A7.1 and P1-A7.2 artifacts remain unchanged and are consumed as prior closures.
- P1-A7.3 uses a separate positive seed with an observed SHA-256 identity for the decoded payload.
- The structural adapter order is UTF-8, JSON, base64, path, length, digest and projection.
- Length mismatch has authority before digest mismatch.
- The multi-defect vector contains base64, path, length and projection defects and must return `encoding.noncanonical-base64`.
- The projection requires exactly the closed seven-field P1-A5 carrier and exact chain identity.
- The external route is observed only at its pre-COSE structural boundary; no `go-cose` execution is claimed.
- Thirty canonical observations are required: one positive and nine negative inputs across three routes.
- P1-A7.3 does not close manifest, DSSE, signature, CBOR, COSE or Receipt negative families.
- P1-A7.3 does not imply production-input coverage, universal parser equivalence or trusted artifact identity.

No deviation is accepted by this attestation.
